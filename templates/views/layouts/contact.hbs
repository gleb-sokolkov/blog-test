<!DOCTYPE html>
<html lang="en">

<head>
    {{> meta}}
    <title>CoreDataNet</title>

    <link rel="stylesheet" href="{{isProd '/styles/shared.css' prod}}">
    <link rel="stylesheet" href="{{isProd '/styles/vendor.css' prod}}">

    {{#if prod}}
    {{> yandexMap}}
    {{> metrika}}
    {{/if}}
</head>

<body>

    {{> navbar}}

    <div>
        {{{body}}}
        <div class="up-button">
            <i class="fa fa-arrow-up" aria-hidden="true"></i>
        </div>
    </div>

    {{> footer}}
    {{> sidenav}}

    <script src="{{isProd '/js/shared.bundle.js' prod}}" async defer></script>
    <script src="{{isProd '/js/vendor.bundle.js' prod}}" async defer></script>
</body>

</html>