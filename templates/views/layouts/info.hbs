<!DOCTYPE html>
<html lang="en">

<head>
    {{> meta}}
    <title>CoreDataNet</title>
    <link rel="stylesheet" href="{{isProd '/styles/shared.css' prod}}">
    <link rel="stylesheet" href="{{isProd '/styles/vendor.css' prod}}">

	{{#if prod}}
		{{> metrika}}
	{{/if}}
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>
</head>

<body>

    {{> navbar}}

    <div>
        {{{body}}}
        <div class="up-button">
            <i class="fa fa-arrow-up" aria-hidden="true"></i>
        </div>
    </div>

    {{> footer}}
    {{> sidenav}}

    <script src="{{isProd '/js/vendor.bundle.js' prod}}"></script>
    <script src="{{isProd '/js/shared.bundle.js' prod}}"></script>
</body>

</html>